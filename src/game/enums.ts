export enum Direction {
  Up = "up",
  Down = "down",
  Right = "right",
  Left = "left"
}

export enum Spell {
  Abara = "abara",
  Kadabara = "kadabara"
}

export enum CheatCode {
  Konami = "up up down down left right left right B A Start"
}
